# Copy to docker/.env and fill in — docker compose reads .env automatically.
# See README for how to create the GitHub OAuth App.
GITHUB_ID=
GITHUB_SECRET=

# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=
